---
datasets: 
   example_dataset:
      lanes: [1]
      
references:
   fasta: "/{reference-path}"
   bwa: "/{bwa-indexed-reference-path}"
   inv: "/{inversion-reference-path}"
   annotation: "/{annotation-path}"
   fasta2: "/{homosapiens-toplevel-path}"

tools:
   vep_cache: "/{vep-cache_homosapiens_98}"

parameters:
   vep-version: "two digit number of vep database version {recommended version 98}"

GATK:
  - gatk4
#  - gatk3
...
  

  
