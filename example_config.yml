---
datasets: 
   dataset1:
      lanes: [1,2,3,4]
   dataset2:
      lanes: [1]
      
references:
   fasta: "/{reference-path}"
   bwa: "/{bwa-indexed-reference-path}"
   inv: "/{inversion-reference-path}"
   annotation: "/{annotation-path}"
   fasta2: "/{homosapiens-toplevel-path}"

shedskin: "/{shedskin-configuration-path"

tools:
   vep: "/{vep-installation-path}"

parameters:
   vep-version: "two digit number of vep database version {recommended version 77}"

GATK:
  - gatk4
#  - gatk3
...
  

  
